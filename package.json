{
  "name": "school-lunch-orders",
  "private": true,
  "type": "module",
  "description": "Hot lunch pre-orders for a Newfoundland school: parents order on their phone, the kitchen gets totals with allergens flagged. Live with SAMPLE data; see README.md.",
  "scripts": {
    "demo": "node demo.mjs",
    "test:worker": "cd worker && npm test",
    "test:negative": "cd worker && npm run negative",
    "test:e2e": "cd app && npx playwright test",
    "test:e2e:negative": "cd app && npm run negative:family && npm run negative:staff && npm run negative:journey",
    "check:allergens": "node tools/check-allergen-quotes.mjs && node tools/check-allergen-quotes.mjs --self-test",
    "dev": "npm run demo",
    "test": "npm run test:worker && npm run test:negative && npm run test:e2e && npm run test:e2e:negative && npm run check:allergens",
    "check": "biome ci . && npm test && check-no-personal-data --quiet ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13"
  }
}
